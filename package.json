{
  "name": "cypressauto1",
  "version": "1.0.0",
  "description": "abcd",
  "main": "index.js",
  "scripts": {
    "build": "rollup -c",
    "start": "http-server",
    "test": "cypress run",
    "cypress:open": "cypress open",
    "cypress:run": "cypress run",
    "cypress:record": "cypress run --record --key"

  },
  "author": "abhay s toriya",
  "license": "ISC",
  "dependencies": {
    "cypress": "^14.0.3"
  },
  "devDependencies": {
    "cypress-wait-until": "^3.0.2",
    "cypress-xpath": "^2.0.1"
  }
}
